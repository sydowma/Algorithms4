package sort;

public class Merge {

    public static void merge( Comparable[] a, int start, int end, int mid) {
        int i = start, j = mid + 1;

        for (int k = start; k < end; k++) {
            aux[k] = a[k];
        }
    }
}
